# Copy this file to .env and fill in real values.
# .env is gitignored — NEVER commit real keys (this is your LLM02 mitigation).

# Postgres
POSTGRES_PASSWORD=changeme

# Connection string used by the .NET app
DATABASE_URL=Host=localhost;Port=5432;Database=mikael_rag;Username=mikael;Password=changeme

# Anthropic (the only paid component — used for generation)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# Embedding service (local Python sidecar, see README)
EMBEDDING_SERVICE_URL=http://localhost:8000/embed
